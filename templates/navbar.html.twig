{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_home') }}" title="Home">Accueil </a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}" title="Register">Inscrire un utilisateur </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user_list') }}" title="UsersList">Liste des utilisateurs </a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('add_sortie') }}" title="AddEvent">Ajouter une sortie </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('edit_profile', {id: app.user.id}) }}" title="EditProfil">
                            {% if app.user.photoDeProfil is not null %}
                                <img src="{{ asset("image/small/#{app.user.photoDeProfil}") }}" alt="pdp">
                            {% endif %}
                            My profil </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}"
                           title="Deconnection">Déconnexion </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}" title="Login">Connexion</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="#" title="About us">A propos</a>
                </li>
            </ul>
        </div>
    </nav>

{% endblock %}